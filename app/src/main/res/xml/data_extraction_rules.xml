<?xml version="1.0" encoding="utf-8"?>
<!--
  The API 31+ equivalent of backup_rules.xml, covering both cloud backup and
  device-to-device transfer.

  Both refuse everything, for the same reason: the SQLCipher passphrase is
  wrapped by a hardware-backed Keystore key that does not leave this device, so
  transferred ciphertext would be unopenable on the target - and health records
  should not travel through a channel the user was never asked about.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="external" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="external" path="." />
    </device-transfer>
</data-extraction-rules>
